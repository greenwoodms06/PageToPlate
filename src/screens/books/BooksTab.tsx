// Placeholder (Task 12) — the Books tab (shelf, library packs, CSV import)
// lands in Tasks 23–25.
import { GearLink, PlaceholderScreen } from '../Placeholder';

export function BooksTab() {
  return <PlaceholderScreen title="Books" stub="Shelf, library packs and CSV import land in Tasks 23–25." action={<GearLink />} />;
}
