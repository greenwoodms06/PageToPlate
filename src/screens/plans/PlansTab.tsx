// Placeholder (Task 12) — the Plans list and calendar land in Tasks 19–20.
import { GearLink, PlaceholderScreen } from '../Placeholder';

export function PlansTab() {
  return <PlaceholderScreen title="Plans" stub="Plans list and calendar land in Tasks 19–20." action={<GearLink />} />;
}
