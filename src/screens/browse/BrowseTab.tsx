// Placeholder (Task 12) — the Browse tab lands in Task 21.
import { GearLink, PlaceholderScreen } from '../Placeholder';

export function BrowseTab() {
  return <PlaceholderScreen title="Browse" stub="Browse with pill search lands in Task 21." action={<GearLink />} />;
}
